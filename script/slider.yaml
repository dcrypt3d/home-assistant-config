#thermo_slider_low:
#  alias: "Update high target from slider"
#  sequence:
#    - service: thermostat.set_target_temp_low
#      data_template:
#        target_temp_low: {{ states.input_slider.thermo_low.state }}
#
#    - service: input_slider.select_value
#      data:
#        entity_id: input_slider.thermo_high
#        value_template: {{ states.thermostat.home.attributes.target_temp_high }}

#thermo_slider_high:
#  alias: "Update high target from slider"
#  sequence:
#    - service: thermostat.set_target_temp_high
#      data_template:
#        target_temp_high: {{ states.input_slider.thermo_high.state }}
#
#    - service: input_slider.select_value
#      data:
#        entity_id: input_slider.thermo_low
#        value_template: {{ states.thermostat.home.attributes.target_temp_low }}
